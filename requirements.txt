streamlit==1.28.0
pandas==1.4.4      # 降低 pandas 版本，避免构建问题
numpy==1.24.3      # numpy 1.24.0+ 不依赖 distutils，但需确保 pandas 兼容
scikit-learn==1.2.2  # 降低 scikit-learn 版本，避免 Python 3.12+ 兼容性问题
xgboost==1.7.2      # 稳定版本，兼容 scikit-learn 1.2.2
matplotlib==3.6.2   # 降低 matplotlib 版本，避免依赖冲突
shap==0.42.1        # 稳定版本，兼容 scikit-learn 1.2.2
plotly==5.15.0      # 稳定版本，避免兼容性问题
joblib==1.2.0       # 稳定版本
